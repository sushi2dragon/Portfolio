# Copy to .env.local for local overrides (optional — dev uses the Vite proxy by default)

# Full URL of the backend API, no trailing slash.
# Leave empty in dev (Vite proxy handles it).
# Set to your Render URL in production, e.g.:
# VITE_API_URL=https://your-app.onrender.com
VITE_API_URL=
